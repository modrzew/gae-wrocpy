from setuptools import (
    find_packages,
    setup,
)

install_requires = [
    'Flask'
]

setup(
    name='wrocpy-flask',
    version='1.0',
    description='',
    author='',
    packages=find_packages(''),
    install_requires=install_requires,
    tests_require=None,
    dependency_links=[],
    entry_points=None,
)
